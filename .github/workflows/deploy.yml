name: Deploy Customer

on:
  push:
    branches: [main]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install --force

      - name: Build
        run: npx ng build --configuration production

      - name: Deploy to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "dist/"
          target: "/var/www/customer/"
          rm: true

      - name: Fix Nginx root & reload
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            DIST_DIR=$(ls /var/www/customer/dist/ | head -1)
            echo "Customer dist: $DIST_DIR"
            
            sed -i "s|root /var/www/customer/dist/.*/browser;|root /var/www/customer/dist/${DIST_DIR}/browser;|g" /etc/nginx/sites-available/shelgo.conf
            
            cp /var/www/customer-config.json /var/www/customer/dist/${DIST_DIR}/browser/assets/configuration.json
            
            nginx -t && systemctl reload nginx
            echo "Customer deployed successfully"
